# Auto-generated by init.sh (contains employee-specific data)
config.json

# Shareable config written by init.sh --export (projects/tasks only).
# Still company-internal data — share privately, don't commit.
config.shared.json

# Team review rules (contains team-specific data)
review-config.json
